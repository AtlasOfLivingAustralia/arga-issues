{
    "dbType": "script",
    "script": {
        "path": "sourceProcessing/reefgenomics.py",
        "function": "build",
        "args": [
            "{PATH, DOWNLOAD}",
            ["project", "organism"]
        ],
        "outputs": [
            "{PATH, DOWNLOAD, project.csv}",
            "{PATH, DOWNLOAD, organism.csv}"
        ]
    }
}
